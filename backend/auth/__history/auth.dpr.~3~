program auth;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  Horse,
  Horse.Jhonson,
  Controller.Auth in 'src\Controller.Auth.pas';

begin
  THorse.Use(Jhonson());

  Controller.Auth.Registry;

  THorse.Listen(9000);
end.
